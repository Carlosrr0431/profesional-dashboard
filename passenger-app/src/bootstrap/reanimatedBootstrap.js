import { ReducedMotionManager } from 'react-native-reanimated/lib/module/ReducedMotion';

// Emulador/dispositivo con "reduced motion" activo: mantener animaciones de la app.
ReducedMotionManager.setEnabled(false);
